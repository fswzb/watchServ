#!python
#-*-coding: utf-8 -*-
'''
    如果有更新，最迟10s会自动重新加载此文件
    监视测试命令配置，启动后会每隔指定秒执行一次命令
    如果命令返回值不为0或者标准输出、错误输出中有任何输出，则把当前时间、返回值、标准输出及错误输出通过邮件发送给指定监视者；
    如果有修复命令，则执行并把修复命令的标准输出和错误输出添加到邮件内容中
'''

# 配置项为：
# 发送到邮箱地址；检测时间间隔秒数；检测命令；是否处理标准输出，1表示处理，0表示不处理；监控到有问题了，需要运行的命令，为空则不处理
cmds = [
    ('zxjcr@outlook.com', 60, '''curl 'http://120.26.105.54:8089/user_app-userwebsite-ui-war-1.0/test.cgi?testDo1=23&testDo2=23&testDo3=21' 2>/dev/null''', 0, '')
    ]

